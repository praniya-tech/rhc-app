window.addEventListener("load", function () {
  //var $ = django.jQuery

  $(document).ready(function () {
    //alert(RHCAPP['CRF_PATIENT_PK']);
    const CRF_PATIENT_PK = RHCAPP['CRF_PATIENT_PK'];

    if (CRF_PATIENT_PK !== undefined) {
      $.ajax({
        url:
      })
    }
  });
});